{% extends "SolustatTimeSheetBundle::layout.html.twig" %}

{% block title %}
  Modifier un employé(e) - {{ parent() }}
{% endblock %}

{% block solustattimesheet_body %}

  <h2>Modifier un employé(e)</h2>

  {{ include("SolustatTimeSheetBundle:Nurse:form.html.twig") }}

  <p>
    Vous éditez un(e) employé(e) déjà existante.
  </p>

  <p>
    <a href="{{ path('solustat_time_sheet_nurse_view', {'id': nurse.id}) }}" class="btn btn-default">
      <i class="glyphicon glyphicon-chevron-left"></i>
      Retour à la fiche
    </a>
  </p>

{% endblock %}