<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <title>{% block title %}Solustat : Emploi du temps pour les libéraux{% endblock %}</title>

    {% block stylesheets %}
        {# On charge le CSS de bootstrap depuis le site directement #}
        <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css">
    {% endblock %}
</head>

<body>
<div class="container">
    <div id="header" class="jumbotron">
        <h1>Ma plateforme des libéraux</h1>
        <p>
            Ce projet <e></e>st propulsé par JVi,
        </p>
    </div>
        <div class="row">
            <div id="menu" class="col-md-3">


                <h3>Les annonces</h3>
                <ul class="nav nav-pills nav-stacked">
                    {#<li><a href="{{ path('solustat_time_sheet_dashboard') }}">Tableau de bord</a></li>#}
                    {#<li><a href="{{ path('solustat_time_sheet_nurse_myaccount') }}">Mon compte</a></li>#}
                    <li><a href="{{ path('solustat_time_sheet_nurse_list', {'page': 1}) }}">Liste du personnel</a>
                        <li><a href="{{ path('solustat_time_sheet_nurse_add') }}">Ajouter un(e) emloyé(e)</a></li>
                    <li><a href="{{ path('solustat_time_sheet_patient_list', {'page': 1}) }}">Liste des patients</a></li>
                    <li><a href="{{ path('solustat_time_sheet_patient_add') }}">Ajouter un patient</a></li>
                    {#<li><a href="{{ path('solustat_time_sheet_bucket_list') }}">Patients à voir</a></li>#}
                    {#<li><a href="{{ path('solustat_time_sheet_nurse_day') }}">Mon emploi du temps</a></li>#}
                </ul>
                <h4>Patient à voir en urgence</h4>
                {#{{ render(controller("OCPlatformBundle:Patient:menu", {'limit': 3})) }}#}
            </div>
            <div id="content" class="col-md-9">
                {% block body %}
                {% endblock %}
            </div>
        </div>


    <hr>
    <footer>
        <p>© {{ 'now'|date('Y') }}</p>
    </footer>
</div>

{% block javascripts %}
    {# Ajoutez ces lignes JavaScript si vous comptez vous servir des fonctionnalités du bootstrap Twitter #}
    <script src="//ajax.googleapis.com/ajax/libs/jquery/1.11.1/jquery.min.js"></script>
    <script src="//maxcdn.bootstrapcdn.com/bootstrap/3.2.0/js/bootstrap.min.js"></script>
{% endblock %}

</body>
</html>